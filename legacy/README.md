# Legacy

This directory contains the original prototype script used to validate the scraping approach.

## scrape_acne_to_bq.py

Original self-contained scraper for Acne Studios and Jil Sander (fashion brands). Written as a single-file proof of concept before the pipeline was refactored into the modular `scrapers/`, `pipelines/`, and `utils/` structure.

Not maintained. Kept for reference only.
